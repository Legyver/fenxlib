package com.legyver.fenxlib.api.controls.options;

import javafx.css.Styleable;

/**
 * Tagging interface for options that produce a control of type Styleable
 * @param <S> the type of the control being produced
 */
public interface StyleableControlOptions<S extends Styleable> extends ControlOptions<S> {
}
