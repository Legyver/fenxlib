allprojects {

    ext {
        //legyver dependencies
        legyverCoreVersion = '1.1.1.1'
        legyverUtilsVersion = '2.0.0.0'
        //apache dependencies
        commonsLogVersion = '2.13.3'
        commonsIoVersion = '2.7'
        commonsLangVersion = '3.11'
        //JFoenix version
        jfoenixVersion = '9.0.10'

        publishBuildScript = 'https://raw.githubusercontent.com/Legyver/common-build/1.2/publish.gradle' //java 9+
    }

    repositories {
        mavenLocal()
        mavenCentral()
        jcenter()
    }

}
