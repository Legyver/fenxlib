allprojects {

    ext {
        //legyver dependencies
        legyverCoreVersion = '1.0.0.0'
        legyverUtilsVersion = '2.0.0.0-alpha-3'
        //apache dependencies
        commonsLogVersion = '2.13.3'
        commonsIoVersion = '2.7'
        commonsLangVersion = '3.11'
        //JFoenix version
        jfoenixVersion = '8.0.7'

        publishBuildScript = 'https://raw.githubusercontent.com/Legyver/common-build/1.1/publish.gradle' //production version
    }

    repositories {
        mavenLocal()
        mavenCentral()
        jcenter()
    }

}


